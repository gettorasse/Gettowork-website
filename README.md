# Get to Work

React, TypeScript, Vite, and Tailwind CSS landing page with a mouse-controlled video background.

## Local development

```sh
npm ci
npm run dev
```

## Build

```sh
npm run build
```

Vercel uses `npm run build` and serves `dist`. The source video and logo are in `public/`.
